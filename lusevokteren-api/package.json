{
  "name": "lusevokteren-api",
  "version": "1.1.0",
  "description": "REST API for FjordVind Lusevokteren - Sea Lice Monitoring",
  "main": "server.js",
  "scripts": {
    "start": "NODE_ENV=production node server.js",
    "dev": "NODE_ENV=development nodemon server.js",
    "start:prod": "NODE_ENV=production node server.js",
    "lint": "eslint .",
    "test": "jest",
    "test:load:smoke": "npx artillery run tests/load/smoke.yml",
    "test:load": "npx artillery run tests/load/artillery.yml",
    "test:load:stress": "npx artillery run tests/load/stress.yml",
    "test:load:quick": "npx artillery quick --count 100 --num 10 http://localhost:3000/api/dashboard/overview",
    "generate-secret": "node -e \"console.log(require('crypto').randomBytes(64).toString('hex'))\""
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "compression": "^1.8.1",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^8.2.1",
    "express-validator": "^7.3.1",
    "helmet": "^8.1.0",
    "joi": "^18.0.2",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.0.2",
    "pg": "^8.11.3",
    "swagger-ui-express": "^5.0.1",
    "winston": "^3.19.0",
    "yamljs": "^0.3.0"
  },
  "devDependencies": {
    "jest": "^30.2.0",
    "nodemon": "^3.0.2",
    "supertest": "^7.2.2"
  },
  "optionalDependencies": {
    "ioredis": "^5.4.2"
  }
}
