# ============================================================
# FjordVind Lusevokteren - Miljøvariabler
# ============================================================
# SIKKERHET: Kopier denne filen til .env og fyll inn verdier.
# ALDRI commit .env til git - den inneholder hemmeligheter!
# ============================================================

# Miljø (development, staging, production)
NODE_ENV=development

# ============================================================
# DATABASE (PostgreSQL)
# ============================================================
POSTGRES_DB=lusevokteren
POSTGRES_USER=postgres
# PÅKREVD: Generer et sterkt passord (minst 16 tegn)
# Eksempel: openssl rand -base64 24
POSTGRES_PASSWORD=

# Ekstern port for database (valgfritt, standard: 5432)
DB_EXTERNAL_PORT=5432

# ============================================================
# AUTENTISERING (JWT)
# ============================================================
# PÅKREVD: Generer en sikker hemmelighet (minst 32 tegn)
# Eksempel: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=

# Token utløpstid (standard: 7d)
JWT_EXPIRES_IN=7d

# ============================================================
# API KONFIGURASJON
# ============================================================
API_PORT=3000
CORS_ORIGIN=http://localhost:5173

# ============================================================
# WEB FRONTEND
# ============================================================
WEB_PORT=80
VITE_API_URL=http://localhost:3000

# ============================================================
# UTVIKLINGSVERKTØY (kun for utvikling)
# ============================================================
# Adminer database UI port
ADMINER_PORT=8080

# Demo-modus (ALDRI aktiver i produksjon!)
# Sett til 'true' kun for lokal utvikling
DEMO_MODE=false

# ============================================================
# BARENTSWATCH API (valgfritt)
# ============================================================
# Registrer app på: https://www.barentswatch.no/minside/
BARENTSWATCH_CLIENT_ID=
BARENTSWATCH_CLIENT_SECRET=

# ============================================================
# E-POST (påkrevd for passord-reset og verifisering)
# ============================================================
# Velg provider: 'sendgrid', 'smtp', eller 'console' (for utvikling)
EMAIL_PROVIDER=console

# SendGrid (anbefalt for produksjon)
# Opprett konto på: https://sendgrid.com/
SENDGRID_API_KEY=

# SMTP (alternativ)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Avsender-info
EMAIL_FROM=noreply@fjordvind.no
EMAIL_FROM_NAME=FjordVind Lusevokteren

# App URL (for lenker i e-post)
APP_URL=http://localhost:5173
API_URL=http://localhost:3000

# ============================================================
# OVERVÅKING (valgfritt, anbefalt for produksjon)
# ============================================================
# Sentry DSN for feilsporing - opprett konto på https://sentry.io
# API (backend)
SENTRY_DSN=
# Frontend (må ha VITE_ prefix for Vite)
VITE_SENTRY_DSN=
# App-versjon for release tracking
VITE_APP_VERSION=1.0.0

# ============================================================
# PRODUKSJON (HTTPS/SSL)
# ============================================================
# Domene for SSL-sertifikat
DOMAIN=lusevokteren.fjordvind.no

# Aktiver HTTPS (sett til 'true' i produksjon)
FORCE_HTTPS=false

# Let's Encrypt e-post (for sertifikat-varsler)
SSL_EMAIL=admin@fjordvind.no

# For å sette opp SSL:
# ./deploy.sh ssl-setup lusevokteren.fjordvind.no admin@fjordvind.no
